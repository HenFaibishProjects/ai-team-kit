services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: virtual-team-kit-postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=ai_team_kit
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - virtual-team-kit-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend API
  backend:
    image: virtual-team-kit-backend:latest
    container_name: virtual-team-kit-backend
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USERNAME=postgres
      - DB_PASSWORD=postgres
      - DB_DATABASE=ai_team_kit
    networks:
      - virtual-team-kit-network
    depends_on:
      postgres:
        condition: service_healthy

  # Frontend Application
  frontend:
    image: virtual-team-kit-frontend:latest
    container_name: virtual-team-kit-frontend
    networks:
      - virtual-team-kit-network
    depends_on:
      - backend

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: virtual-team-kit-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
    networks:
      - virtual-team-kit-network
    depends_on:
      - frontend
      - backend
    restart: unless-stopped

networks:
  virtual-team-kit-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
